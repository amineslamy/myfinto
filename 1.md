در ادامه شرح متمرکز و مستندِ علت‌ها و رفع مشکل به زبان فارسی — کوتاه، دقیق و با قدم‌های جلوگیری در آینده.

خلاصهٔ کوتاه

علت اصلی: مجموعه‌ای از مسائل پیکربندی و فایل‌های لازم برای Gradle که با هم تداخل داشتند (نداشتن Gradle wrapper/JAR، نداشتن بیت اجرایی روی gradlew، پیکربندی‌های نادرست workflow و تنظیم امضای debug). خطاها به‌تدریج برطرف شدند و در تلاش نهایی (دهم) با تنظیمات صحیح، build موفق شد و شما APK را دریافت کردید (sha256:55d55e...fca26).
شرح زمان‌بندی و دلایل شکست‌های قبلی

تلاش‌های اولیه (۱–۳): workflow اولیه حاوی ارجاع به GEMINI_API_KEY و استفاده از actionهای نسخه‌های قدیمی یا ورودی‌های نادرست بود؛ این باعث خطاهای immediate و هشدارهای deprecated شد.
تلاش‌های میانی (۴–۷): نبودن Gradle wrapper در repo و عدم وجود gradle-wrapper.jar باعث شد runner یا محلی نتواند gradlew را اجرا کند (خطاهایی مثل "Could not find or load main class org.gradle.wrapper.GradleWrapperMain" یا خطاهای مربوط به gradle محلی).
تلاش‌های بعدی (۸–۹): پس از اضافه‌کردن اسکریپت‌های wrapper، فایل gradlew بدون بیت اجرایی بود → در runner لینوکس خطا با exit code 126 (Permission denied). همچنین workflow از نسخه‌ها/ورودی‌های نامعتبر action استفاده می‌کرد (مثلاً actions/upload-artifact@v3 منسوخ، یا android-actions/setup-android با ورودی‌های غیرمعتبر) و cache service GitHub گاهی خطا می‌داد (پیام‌های "Our services aren't available right now") — این خطاهای cache گذرا بودند اما باعث سردرگمی شدند.
تلاش ۱۰ (موفق): ترکیبی از اصلاحات اعمال شد:
فایل‌های wrapper (gradlew, gradlew.bat, gradle-wrapper.properties) اضافه شدند،
اجازهٔ اجرایی (chmod +x gradlew) و در git تنظیم شد (git update-index --chmod=+x gradlew),
workflow به actionهای سازگار و به‌روز تغییر کرد (actions/checkout@v4, actions/setup-java@v4, android-actions/setup-android@v4, gradle/actions/setup-gradle@v3, actions/upload-artifact@v4) یا به‌جای reliance به یک missing jar از gradle action استفاده شد،
تنظیم امضای debug در [app/build.gradle.kts#L1](http://_vscodecontentref_/7) اصلاح شد تا به debug.keystore غیرموجود وابسته نباشد.
نتیجه: runner اکنون می‌توانست محیط را آماده کند و build کامل اجرا شد.
اشکال‌های اصلی دقیق (ریشه‌ای)

عدم وجود Gradle wrapper کامل در repo (خصوصاً gradle-wrapper.jar) → wrapper script به‌تنهایی کافی نیست.
نبود بیت اجرایی روی gradlew در گیت → exit code 126 (permission denied).
پیکربندی workflow با actionهای منسوخ یا ورودی‌های نادرست → خطاهای immediate مانند "Unexpected input(s) ...".
تنظیم signing در build.gradle.kts که به debug.keystore اشاره می‌کرد اما فایل وجود نداشت → build برای امضای apk مشکل داشت.
خطاهای cache و سرویس GitHub (پیام «Our services aren't available right now») → اختلال زیرساختی و گذرا که باعث شد بعضی runs نتوانند از cache استفاده کنند یا ذخیره کنند؛ معمولاً غیرمرگبار اما موجب ناپایداری شد.
(کم‌اهمیت) وابستگی‌ها و متغیرهای محیطی مانند GEMINI_API_KEY که ابتدا در workflow پیش‌بینی شده بود — نداشتن آن ممکن بود باعث خطا در مراحل خاص شود اگر کد به آن ارجاع می‌داد؛ شما تصمیم گرفتید آن را حذف کردیم چون کد از آن استفاده نمی‌کرد.
چرا تلاش دهم جواب داد (جمع‌بندی علت موفقیت)

تمام موانع اجرایی حذف یا دور زده شدند: wrapper یا به‌درستی در repo فراهم شد یا از setup-gradle استفاده شد؛ gradlew اجرایی شد؛ workflow با actionهای صحیح به‌روز شد؛ و پیکربندی امضا اصلاح گردید.
بنابراین runner توانست محیط (JDK، Android SDK) را آماده کرده، Gradle را اجرا کند و خروجی APK را بسازد و آپلود کند.
فایل‌ها و تغییرات کلیدی (برای مرجع)

Workflow: build.yml:1 — نسخه‌ها و گام‌ها به‌روز شدند، chmod +x gradlew اضافه شد، سپس در اصلاحات بعدی از gradle/actions/setup-gradle@v3 یا از wrapper ثابت استفاده شد.
Gradle config: build.gradle.kts:1 — حذف/اصلاح debugConfig که به debug.keystore وابسته بود.
Wrapper files: gradlew, gradlew.bat, gradle-wrapper.properties:1 (و در حالت ایده‌آل gradle/wrapper/gradle-wrapper.jar).
اقدامات پیشنهادی برای جلوگیری از تکرار این مشکل

همیشه Gradle wrapper را در repo داشته باشید (تولید با محلی):

./gradlew wrapper --gradle-version 8.4git add gradlew gradlew.bat gradle/wrapper/*git commit -m "Add Gradle wrapper"
مطمئن شوید gradlew در گیت بیت اجرایی دارد:

git update-index --add --chmod=+x gradlew
قبل از push، یک build محلی اجرا کنید تا خطاهای کامپایل و dependency را بگیرید:

./gradlew assembleDebug --no-daemon
(نیاز به JDK و Android SDK محلی دارد؛ اگر نمی‌خواهید Android Studio نصب کنید، نصب cmdline-tools و platform-tools از Android SDK کافیست.)
در workflow:
از نسخه‌های پایدار و به‌روز actions استفاده و ورودی‌ها را مطابق README هر action تنظیم کنید.
gradle را یا با wrapper (که در repo است) اجرا کنید یا از gradle/actions/setup-gradle استفاده کنید — یکی را انتخاب کنید و ثابت نگه دارید.
قبل از نیاز به keystore خصوصی، از امضای debug پیش‌فرض استفاده کنید یا keystore را با امن (secrets) در repo/organization ذخیره کنید.
برای خطاهای cache یا سرویس GitHub: اگر log نشان می‌دهد cache مشکل دارد، معمولاً re-run job حل می‌کند؛ اما وابسته‌سازی کامل build به cache نکنید.
اضافه کردن یک step سادهٔ تستی در workflow برای چاپ نسخه‌ها و مسیرها، اگر بخواهید دیباگ سریع‌تر باشد:

- name: Env info  run: |    java -version    uname -a    ./gradlew -v
نگهداری یک checklist قبل از push: wrapper موجود، gradlew اجرایی، build محلی پاک اجرا می‌شود، secrets لازم تنظیم شده‌اند.
چک‌لیست سریع برای دفعۀ بعد (قابل انجام در <10 دقیقه)

 ./gradlew assembleDebug محلی اجرا شود
 gradlew در repo وجود داشته باشد و chmod +x داشته باشد
 workflow از actionهای به‌روز و ورودی‌های صحیح استفاده کند
 build.gradle.kts تنظیم signing معقول داشته باشد (یا keystore برای release را از Secrets بارگیری کند)
 در صورت بروز خطا، بخش آخرین 20–50 خط console log را کپی کنید و بررسی کنید آیا ارور مربوط به شبکه، dependency، یا compile است
اگر بخواهید، من می‌توانم این چک‌لیست را به‌صورت یک GitHub Actions preflight job اضافه کنم که قبل از build اصلی اجرا شود و خطاهای رایج را زودتر نشان دهد — مایلید این را اضافه کنم؟

